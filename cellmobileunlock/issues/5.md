# Database Normalization

Database normalization is the process of organizing a database to reduce redundancy and improve data integrity. Here we cover the detailed implementation examples, including table creation statements, sample data insertion, query examples, benefits, and a migration strategy.

## 1. Table Creation Statements

### Example: Normalizing a Customer Database

```sql
CREATE TABLE Customers (
    CustomerID INT PRIMARY KEY,
    FirstName VARCHAR(50),
    LastName VARCHAR(50),
    Email VARCHAR(100)
);

CREATE TABLE Orders (
    OrderID INT PRIMARY KEY,
    OrderDate DATETIME,
    CustomerID INT,
    FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID)
);
```

## 2. Sample Data Insertion

### Inserting Data into Customers and Orders

```sql
INSERT INTO Customers (CustomerID, FirstName, LastName, Email) VALUES
(1, 'John', 'Doe', 'john.doe@example.com'),
(2, 'Jane', 'Smith', 'jane.smith@example.com');

INSERT INTO Orders (OrderID, OrderDate, CustomerID) VALUES
(1, '2026-03-01 12:00:00', 1),
(2, '2026-03-05 14:30:00', 2);
```

## 3. Query Examples

### Fetching Data

```sql
SELECT Customers.FirstName, Customers.LastName, Orders.OrderDate
FROM Customers
JOIN Orders ON Customers.CustomerID = Orders.CustomerID;
```

## 4. Benefits of Database Normalization
- **Reduced Redundancy**: Minimizes duplicate data.
- **Improved Data Integrity**: Ensures consistent data across the database.
- **Easier Maintenance**: Changes are easier to implement with a normalized structure.

## 5. Migration Strategy
- **Step 1**: Analyze existing database and identify redundancies.
- **Step 2**: Create normalized tables based on analysis.
- **Step 3**: Migrate existing data into new tables using ETL processes.
- **Step 4**: Perform testing to ensure data integrity post-migration.
- **Step 5**: Retire old database structure once migration is validated.

## References
- [Database Normalization Explained](https://www.databasejournal.com/features/db2/database-normalization-explained.html)
- [ETL Process](https://www.talend.com/resources/what-is-etl/)